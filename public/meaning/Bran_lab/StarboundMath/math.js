/* Original, parameterized math problems. No external services or generated-at-play AI. */
(function(root){'use strict';
const realms=[
[4,'Mosslight Meadow','Whole numbers · fractions · shapes','#98cead'],[5,'Coral Canopy','Decimals · volume · fraction operations','#f2b69c'],[6,'Crystal Caves','Ratios · expressions · number sense','#adb3ed'],[7,'Cloud Harbor','Proportions · integers · probability','#92d3df'],[8,'Moonstone Ridge','Linear equations · powers · geometry','#b8b2ed'],[9,'Aurora Forest','Algebra I · quadratics · systems','#9bd6c3'],[10,'Sunforge Isles','Geometry · similarity · trigonometry','#ecc584'],[11,'Orbit Observatory','Algebra II · sequences · logarithms','#aab7ee'],[12,'Starlight Summit','Precalculus · introductory calculus','#c7adf0']].map(([grade,name,topics,color])=>({grade,name,topics,color}));
function rng(seed){let t=seed>>>0;return()=>{t+=0x6D2B79F5;let x=Math.imul(t^t>>>15,1|t);x^=x+Math.imul(x^x>>>7,61|x);return((x^x>>>14)>>>0)/4294967296;};}
const gcd=(a,b)=>b?gcd(b,a%b):a,choose=(n,k)=>{let x=1;for(let j=1;j<=k;j++)x=x*(n-j+1)/j;return Math.round(x);};
function generate(grade,kind,index,seed){const rand=rng(seed),r=(a,b)=>a+Math.floor(rand()*(b-a+1)),a=r(2,9),b=r(2,8),c=r(2,6),n=r(3,7);let topic,prompt,answer,hint,steps;
const set=(t,p,v,h,s)=>{topic=t;prompt=p;answer=v;hint=h;steps=s;};
const challenge=kind==='challenge',i=((index%(challenge?3:6))+(challenge?3:6))%(challenge?3:6);
if(!challenge)switch(grade){
case 4:switch(i){
case 0:set('Multiplication',`${a} supply crates each hold ${b*10+c} star seeds. How many seeds are there?`,a*(b*10+c),'Split the two-digit number into tens and ones.',[`${a} × ${b*10} = ${a*b*10}.`,`${a} × ${c} = ${a*c}.`,`Add: ${a*b*10} + ${a*c} = ${a*(b*10+c)}.`]);break;
case 1:set('Division',`${a*b*c} gems are shared equally among ${a} explorers. How many does each get?`,b*c,'Division undoes multiplication.',[`${a} × ${b*c} = ${a*b*c}.`,`So ${a*b*c} ÷ ${a} = ${b*c}.`]);break;
case 2:set('Fractions',`What is 1/${a} of ${a*b*3}?`,b*3,'Divide the whole into equal groups.',[`${a*b*3} ÷ ${a} = ${b*3}.`,`One of the ${a} equal parts is ${b*3}.`]);break;
case 3:set('Perimeter',`A rectangular garden is ${a+5} m long and ${b} m wide. What is its perimeter in metres?`,2*(a+5+b),'Walk around all four sides.',[`P = 2 × (length + width).`,`2 × (${a+5} + ${b}) = ${2*(a+5+b)} m.`]);break;
case 4:set('Patterns',`A trail has markers at ${a}, ${a+b}, ${a+2*b}, ${a+3*b}, … . What is the next number?`,a+4*b,'Find the amount added each time.',[`Each step adds ${b}.`,`${a+3*b} + ${b} = ${a+4*b}.`]);break;
case 5:set('Area',`A rectangle has area ${a*b} square metres and width ${b} m. How long is it, in metres?`,a,'Area = length × width.',[`Length = area ÷ width.`,`${a*b} ÷ ${b} = ${a} m.`]);break;}break;
case 5:switch(i){
case 0:set('Decimals',`What is ${(a/10).toFixed(1)} + ${(b/100).toFixed(2)}?`,a/10+b/100,'Write both amounts in hundredths.',[`${(a/10).toFixed(2)} + ${(b/100).toFixed(2)} = ${((a*10+b)/100).toFixed(2)}.`]);break;
case 1:set('Fraction addition',`What is 1/${a} + 1/${a*2}? Enter a fraction or decimal.`,3/(a*2),'Use the larger denominator for both fractions.',[`1/${a} = 2/${a*2}.`,`2/${a*2} + 1/${a*2} = 3/${a*2}.`]);break;
case 2:set('Fraction multiplication',`What is ${a}/${a+1} of ${b*(a+1)}?`,a*b,'Divide by the denominator, then multiply by the numerator.',[`${b*(a+1)} ÷ ${a+1} = ${b}.`,`${b} × ${a} = ${a*b}.`]);break;
case 3:set('Volume',`A box is ${a} cm by ${b} cm by ${c} cm. What is its volume in cubic centimetres?`,a*b*c,'Multiply all three dimensions.',[`V = length × width × height.`,`${a} × ${b} × ${c} = ${a*b*c} cm³.`]);break;
case 4:set('Decimal division',`What is ${(a*b/10).toFixed(1)} ÷ ${a}?`,b/10,'Think in tenths first.',[`${a*b} tenths ÷ ${a} = ${b} tenths.`,`That is ${(b/10).toFixed(1)}.`]);break;
case 5:set('Fraction division',`How many pieces of length 1/${b} metre can be cut from ${a} metres?`,a*b,'Each whole metre contains several equal pieces.',[`One metre contains ${b} pieces.`,`${a} × ${b} = ${a*b} pieces.`]);break;}break;
case 6:switch(i){
case 0:set('Ratios',`A mix has red and blue beads in ratio ${a}:${b}. There are ${a*c} red beads. How many blue beads?`,b*c,'Use the same scale factor for both parts.',[`The scale factor is ${a*c} ÷ ${a} = ${c}.`,`Blue = ${b} × ${c} = ${b*c}.`]);break;
case 1:set('Percentages',`What is ${b*10}% of ${a*10}?`,a*b,'Percent means out of 100.',[`${b*10}/100 × ${a*10} = ${a*b}.`]);break;
case 2:set('Equations',`Solve for x: ${a}x + ${b} = ${a*c+b}.`,c,'Undo addition, then multiplication.',[`Subtract ${b}: ${a}x = ${a*c}.`,`Divide by ${a}: x = ${c}.`]);break;
case 3:set('Mean',`Find the mean of ${a}, ${a+b}, and ${a+2*b}.`,a+b,'Add the values and divide by 3.',[`Sum = ${3*a+3*b}.`,`${3*a+3*b} ÷ 3 = ${a+b}.`]);break;
case 4:set('Greatest common divisor',`What is the greatest common divisor of ${a*c} and ${b*c}?`,gcd(a*c,b*c),'Look for the largest whole number that divides both.',[`The GCD of ${a} and ${b} is ${gcd(a,b)}.`,`Multiply by the common factor ${c}: ${gcd(a,b)*c}.`]);break;
case 5:set('Triangle area',`A triangle has base ${a*2} cm and perpendicular height ${b} cm. What is its area in cm²?`,a*b,'A triangle fills half a matching rectangle.',[`Area = base × height ÷ 2.`,`${a*2} × ${b} ÷ 2 = ${a*b}.`]);break;}break;
case 7:switch(i){
case 0:set('Signed numbers',`What is (−${a}) × ${b} + ${c}?`,-a*b+c,'Multiply before adding.',[`A negative times a positive is negative: −${a*b}.`,`−${a*b} + ${c} = ${-a*b+c}.`]);break;
case 1:set('Proportions',`${a} notebooks cost ${a*b} coins. At that rate, how many coins do ${c} notebooks cost?`,b*c,'Find the cost of one notebook.',[`${a*b} ÷ ${a} = ${b} coins each.`,`${c} × ${b} = ${b*c}.`]);break;
case 2:set('Percent change',`An item costs ${a*20} coins and is reduced by 25%. What is its new price?`,a*15,'Keep 75% of the original price.',[`25% of ${a*20} is ${a*5}.`,`${a*20} − ${a*5} = ${a*15}.`]);break;
case 3:set('Probability',`A bag has ${a} gold and ${b} blue gems. One gem is picked at random. What is the probability of gold? Enter a fraction.`,a/(a+b),'Favourable outcomes divided by all outcomes.',[`There are ${a+b} gems in total.`,`P(gold) = ${a}/${a+b}.`]);break;
case 4:set('Equations',`Solve: ${a}(x − ${b}) = ${a*c}.`,b+c,'Divide both sides before undoing the subtraction.',[`x − ${b} = ${c}.`,`x = ${c} + ${b} = ${b+c}.`]);break;
case 5:set('Angles',`Two angles in a triangle measure ${a*5}° and ${b*5}°. What is the third angle, in degrees?`,180-a*5-b*5,'Triangle angles sum to 180°.',[`180 − ${a*5} − ${b*5} = ${180-a*5-b*5}°.`]);break;}break;
case 8:switch(i){
case 0:set('Linear equations',`Solve: ${a+2}x + ${b} = 2x + ${a*c+b}.`,c,'Gather x terms on one side.',[`Subtract 2x and ${b}: ${a}x = ${a*c}.`,`x = ${c}.`]);break;
case 1:set('Slope',`A line passes through (1, ${b}) and (${a+1}, ${b+a*c}). What is its slope?`,c,'Slope is rise divided by run.',[`Rise = ${a*c}; run = ${a}.`,`Slope = ${a*c}/${a} = ${c}.`]);break;
case 2:set('Pythagoras',`A right triangle has legs ${3*a} and ${4*a}. What is its hypotenuse?`,5*a,'Use a² + b² = c², or scale a 3–4–5 triangle.',[`${3*a}² + ${4*a}² = ${25*a*a}.`,`The positive square root is ${5*a}.`]);break;
case 3:set('Exponent rules',`If 2^${a} × 2^${b} = 2^k, what is k?`,a+b,'Multiplying powers with the same base adds exponents.',[`2^${a} × 2^${b} = 2^(${a}+${b}).`,`k = ${a+b}.`]);break;
case 4:set('Functions',`If f(x) = ${a}x − ${b}, what is f(${c})?`,a*c-b,'Replace x by the input value.',[`f(${c}) = ${a} × ${c} − ${b} = ${a*c-b}.`]);break;
case 5:set('Square roots',`What is the positive square root of ${(a+b)**2}?`,a+b,'Find the positive number whose square is given.',[`${a+b} × ${a+b} = ${(a+b)**2}.`,`The positive square root is ${a+b}.`]);break;}break;
case 9:switch(i){
case 0:set('Quadratic roots',`The equation x² − ${a+b}x + ${a*b} = 0 has two roots, counting repeats. What is their sum?`,a+b,'Factor the quadratic or use the coefficient of x.',[`x² − ${a+b}x + ${a*b} = (x − ${a})(x − ${b}).`,`The roots add to ${a+b}.`]);break;
case 1:set('Systems',`Solve for x: x + y = ${a+b} and x − y = ${a-b}.`,a,'Add the equations to eliminate y.',[`2x = ${2*a}.`,`x = ${a}.`]);break;
case 2:set('Exponential growth',`A colony begins with ${a} cells and doubles ${c} times. How many cells are there?`,a*2**c,'Each doubling multiplies by 2.',[`${a} × 2^${c} = ${a*2**c}.`]);break;
case 3:set('Inequalities',`What is the smallest integer x for which ${a}x > ${a*b}?`,b+1,'Divide by a positive number; keep the inequality direction.',[`x > ${b}.`,`The next integer is ${b+1}.`]);break;
case 4:set('Quadratic minimum',`What is the minimum value of f(x) = (x − ${a})² + ${b}?`,b,'A real square cannot be negative.',[`(x − ${a})² ≥ 0, with equality at x = ${a}.`,`The minimum value is ${b}.`]);break;
case 5:set('Arithmetic sequences',`A sequence starts at ${a} and increases by ${b}. What is its ${n}th term?`,a+(n-1)*b,'The first term has zero increases.',[`aₙ = first + (n − 1) × difference.`,`${a} + (${n} − 1) × ${b} = ${a+(n-1)*b}.`]);break;}break;
case 10:switch(i){
case 0:set('Similarity',`Two similar triangles have scale factor ${c} from small to large. A small side is ${a} cm. What is the matching large side?`,a*c,'Corresponding lengths use the same scale factor.',[`${a} × ${c} = ${a*c} cm.`]);break;
case 1:set('Trigonometry',`In a right triangle, the side opposite θ is ${3*a} and the hypotenuse is ${5*a}. What is sin θ? Enter a fraction.`,.6,'Sine = opposite / hypotenuse.',[`sin θ = ${3*a}/${5*a} = 3/5.`]);break;
case 2:set('Circle area',`A circle has radius ${a}. Its area is kπ. What is k?`,a*a,'The area formula is πr².',[`π × ${a}² = ${a*a}π.`,`k = ${a*a}.`]);break;
case 3:set('Distance',`Find the distance between (${a}, ${b}) and (${a+3*c}, ${b+4*c}).`,5*c,'Use the changes in x and y as right-triangle legs.',[`Δx = ${3*c}, Δy = ${4*c}.`,`Distance = √(${9*c*c} + ${16*c*c}) = ${5*c}.`]);break;
case 4:set('Polygon angles',`What is the sum of the interior angles of a convex ${n+3}-sided polygon, in degrees?`,(n+1)*180,'An n-sided polygon splits into n − 2 triangles.',[`(${n+3} − 2) × 180 = ${(n+1)*180}°.`]);break;
case 5:set('Area scaling',`A shape is enlarged by linear scale factor ${c}. Its original area is ${a}. What is its new area?`,a*c*c,'Both dimensions scale, so area scales by the square.',[`Area scale factor = ${c}² = ${c*c}.`,`${a} × ${c*c} = ${a*c*c}.`]);break;}break;
case 11:switch(i){
case 0:set('Logarithms',`What is log base ${a} of ${a**c}?`,c,'A logarithm asks for the exponent.',[`${a}^${c} = ${a**c}.`,`So log base ${a} of ${a**c} = ${c}.`]);break;
case 1:set('Geometric sequences',`A geometric sequence begins ${a}, ${a*b}, ${a*b*b}, … . What is its 4th term?`,a*b**3,'Multiply by the common ratio again.',[`The ratio is ${b}.`,`${a*b*b} × ${b} = ${a*b**3}.`]);break;
case 2:set('Function composition',`f(x) = ${a}x + 1 and g(x) = x². What is f(g(${c}))?`,a*c*c+1,'Work from the inside outward.',[`g(${c}) = ${c*c}.`,`f(${c*c}) = ${a} × ${c*c} + 1 = ${a*c*c+1}.`]);break;
case 3:set('Complex numbers',`For i² = −1, what is the real part of (${a} + ${b}i)(${a} − ${b}i)?`,a*a+b*b,'These are conjugates. The imaginary terms cancel.',[`Product = ${a*a} − ${b*b}i².`,`Since i² = −1, the result is ${a*a+b*b}.`]);break;
case 4:set('Combinations',`From ${n+3} distinct explorers, how many different teams of 2 can you choose? Order does not matter.`,choose(n+3,2),'Count ordered choices, then divide by the number of orders.',[`${n+3} × ${n+2} ÷ 2 = ${choose(n+3,2)}.`]);break;
case 5:set('Inverse functions',`f(x) = ${a}x + ${b}. What is f⁻¹(${a*c+b})?`,c,'Find the input that produces this output.',[`Undo +${b}, then ×${a}.`,`(${a*c+b} − ${b}) ÷ ${a} = ${c}.`]);break;}break;
case 12:switch(i){
case 0:set('Derivatives',`If f(x) = ${a}x³, what is f′(${c})?`,3*a*c*c,'Use the power rule: d(xⁿ)/dx = nxⁿ⁻¹.',[`f′(x) = ${3*a}x².`,`f′(${c}) = ${3*a} × ${c*c} = ${3*a*c*c}.`]);break;
case 1:set('Definite integrals',`Find the integral of ${2*a}x from x = 0 to x = ${c}.`,a*c*c,'An antiderivative of 2ax is ax².',[`An antiderivative is ${a}x².`,`Evaluate the endpoints: ${a} × ${c}² − 0 = ${a*c*c}.`]);break;
case 2:set('Limits',`As x approaches ${a}, what is the limit of (x² − ${a*a}) / (x − ${a})?`,2*a,'Factor the numerator before taking the limit.',[`x² − ${a*a} = (x − ${a})(x + ${a}).`,`For x ≠ ${a}, the quotient is x + ${a}.`,`Its limit is ${a+a}.`]);break;
case 3:set('Expected value',`A fair ${b}-sided spinner has one gold sector. Each gold result earns ${a*b} points; other results earn 0. What is the expected score per spin?`,a,'Multiply each score by its probability and add.',[`P(gold) = 1/${b}.`,`${a*b} × 1/${b} + 0 = ${a}.`]);break;
case 4:set('Geometric series',`An infinite series begins ${a}, ${a/2}, ${a/4}, … and halves each time. What is its sum?`,2*a,'For |r| < 1, sum = first term / (1 − r).',[`S = ${a} / (1 − 1/2).`,`S = ${2*a}.`]);break;
case 5:set('Stationary points',`For f(x) = x² − ${2*a}x + ${b}, at what x is the derivative zero?`,a,'Differentiate, then set the result equal to zero.',[`f′(x) = 2x − ${2*a}.`,`2x − ${2*a} = 0 gives x = ${a}.`]);break;}break;
}
if(challenge)switch(grade){
case 4:if(i===0)set('Counting rectangles',`How many rectangles of any size are in a grid with 2 rows and ${n} columns?`,3*n*(n+1)/2,'A rectangle is determined by two horizontal and two vertical grid lines.',[`Choose 2 of 3 horizontal lines: 3 ways.`,`Choose 2 of ${n+1} vertical lines: ${n*(n+1)/2} ways.`,`Multiply: ${3*n*(n+1)/2}.`]);else if(i===1)set('Working backwards',`I double a number, add ${b}, then triple the result and get ${3*(2*a+b)}. What was my number?`,a,'Undo the last operation first.',[`Divide by 3: ${2*a+b}.`,`Subtract ${b}: ${2*a}.`,`Divide by 2: ${a}.`]);else set('Pigeonhole principle',`A drawer contains many socks in ${c} colours. Without looking, how many socks guarantee two of the same colour?`,c+1,'Imagine the most unhelpful first draws.',[`The first ${c} socks could all have different colours.`,`The next sock must match one of them: ${c+1} socks.`]);break;
case 5:if(i===0)set('Counting paths',`A robot goes from (0,0) to (${n},2), moving only one step right or one step up. How many shortest paths are there?`,choose(n+2,2),'Choose where the two upward steps occur.',[`There are ${n+2} steps, of which 2 go up.`,`Choose those positions: (${n+2} × ${n+1}) / 2 = ${choose(n+2,2)}.`]);else if(i===1)set('Remainders',`What is the smallest positive number that leaves remainder 1 when divided by 3, 4, and 5, and is greater than 1?`,61,'Subtract 1. The result must be divisible by all three divisors.',[`LCM(3,4,5) = 60.`,`The smallest allowed number is 60 + 1 = 61.`]);else set('Hidden total',`A fox eats 1/3 of its berries, then 1/2 of what remains. It has ${a} berries left. How many did it start with?`,3*a,'Track the fraction of the original berries that survives.',[`After the first snack, 2/3 remain.`,`Half of 2/3 is 1/3.`,`The original amount was ${a} × 3 = ${3*a}.`]);break;
case 6:if(i===0)set('Divisor counting',`How many positive divisors does 2^${c} × 3² have?`,(c+1)*3,'A divisor chooses an exponent for each prime.',[`The exponent of 2 has ${c+1} choices: 0 through ${c}.`,`The exponent of 3 has 3 choices: 0, 1, 2.`,`Total = ${c+1} × 3 = ${(c+1)*3}.`]);else if(i===1)set('Inclusion–exclusion',`How many integers from 1 through ${30*c}, inclusive, are divisible by 2 or 3?`,20*c,'Count each set, then subtract numbers counted twice.',[`Multiples of 2: ${15*c}; multiples of 3: ${10*c}.`,`Multiples of both (6): ${5*c}.`,`${15*c} + ${10*c} − ${5*c} = ${20*c}.`]);else set('Clock meetings',`Two beacons flash together now. One flashes every ${a*2} seconds, the other every ${a*3} seconds. After how many seconds will they next flash together?`,a*6,'Find the least common multiple of the periods.',[`LCM(${a*2}, ${a*3}) = ${a*6}.`,`That is the first positive common flashing time.`]);break;
case 7:if(i===0)set('Remainder cycles',`What is the remainder when 2^${4*n+1} is divided by 5?`,2,'List the first four powers modulo 5.',[`The remainders repeat 2, 4, 3, 1.`,`${4*n+1} leaves remainder 1 on division by 4.`,`So the remainder is 2.`]);else if(i===1)set('Handshakes',`${n+4} explorers each shake hands once with every other explorer. How many handshakes occur?`,choose(n+4,2),'Each handshake is one unordered pair.',[`${n+4} × ${n+3} counts each handshake twice.`,`Divide by 2: ${choose(n+4,2)}.`]);else set('Probability without replacement',`A bag holds ${a} red and ${b} blue gems. Two are drawn without replacement. What is the probability both are red? Enter a fraction.`,a*(a-1)/((a+b)*(a+b-1)),'The second draw has one fewer red gem and one fewer total gem.',[`First red: ${a}/${a+b}.`,`Then red: ${a-1}/${a+b-1}.`,`Multiply: ${a*(a-1)}/${(a+b)*(a+b-1)}.`]);break;
case 8:if(i===0)set('Difference of squares',`Positive integers x and y satisfy x + y = ${2*a+2} and x − y = 2. What is x² − y²?`,4*a+4,'Use the factorization (x − y)(x + y).',[`x² − y² = 2 × ${2*a+2} = ${4*a+4}.`]);else if(i===1)set('Integer solutions',`How many ordered pairs of positive integers (x,y) satisfy x + y = ${a+b}?`,a+b-1,'Choose x; then y is determined.',[`x can be 1 through ${a+b-1}.`,`Each gives one positive y, so there are ${a+b-1} pairs.`]);else set('Square pattern',`What is 1 + 3 + 5 + … + ${2*n-1}?`,n*n,'Arrange consecutive odd groups into growing squares.',[`There are ${n} odd numbers.`,`The sum of the first n odd numbers is n².`,`Here the sum is ${n*n}.`]);break;
case 9:if(i===0)set('Symmetric expressions',`x + y = ${a+b} and xy = ${a*b}. What is x² + y²?`,a*a+b*b,'Expand (x + y)².',[`x² + y² = (x + y)² − 2xy.`,`${a+b}² − 2 × ${a*b} = ${a*a+b*b}.`]);else if(i===1)set('Constrained products',`Positive integers x and y sum to ${2*n}. What is the largest possible value of xy?`,n*n,'Try moving the two numbers closer together.',[`Write x = ${n} + d and y = ${n} − d.`,`Then xy = ${n*n} − d² ≤ ${n*n}.`,`Equality occurs when x = y = ${n}.`]);else set('Remainder theorem',`What is the remainder when x³ + ${b}x + ${c} is divided by x − ${a}?`,a**3+b*a+c,'Evaluate the polynomial at the zero of the divisor.',[`The remainder theorem gives P(${a}).`,`${a}³ + ${b} × ${a} + ${c} = ${a**3+b*a+c}.`]);break;
case 10:if(i===0)set('Diagonals',`How many diagonals does a convex ${n+3}-sided polygon have?`,(n+3)*n/2,'Each vertex connects to every non-neighbouring vertex.',[`From each vertex there are ${n} diagonals.`,`Counting both ends counts each twice.`,`${n+3} × ${n} ÷ 2 = ${(n+3)*n/2}.`]);else if(i===1)set('Incircle geometry',`A right triangle has legs ${3*a}, ${4*a} and hypotenuse ${5*a}. What is its inradius?`,a,'Area = inradius × semiperimeter.',[`Area = ${6*a*a}; semiperimeter = ${6*a}.`,`r = ${6*a*a} ÷ ${6*a} = ${a}.`]);else set('Lattice points',`How many lattice points, including both endpoints, lie on the segment from (0,0) to (${2*a},${3*a})?`,a+1,'The smallest integer step along the segment is (2,3).',[`There are ${a} steps of (2,3).`,`Counting the start and each step gives ${a+1} points.`]);break;
case 11:if(i===0)set('Binomial coefficients',`What is the coefficient of x² in (1 + x)^${n+3}?`,choose(n+3,2),'Choose which two factors contribute an x.',[`The coefficient is C(${n+3},2).`,`${n+3} × ${n+2} ÷ 2 = ${choose(n+3,2)}.`]);else if(i===1)set('Telescoping sum',`Find 1/(1×2) + 1/(2×3) + … + 1/(${n}×${n+1}). Enter a fraction.`,n/(n+1),'Use 1/[k(k+1)] = 1/k − 1/(k+1).',[`The sum becomes (1 − 1/2) + (1/2 − 1/3) + … .`,`All middle terms cancel.`,`Result = 1 − 1/${n+1} = ${n}/${n+1}.`]);else set('Reciprocal symmetry',`For nonzero x, x + 1/x = ${a}. Find x² + 1/x².`,a*a-2,'Square both sides, keeping the middle term.',[`(x + 1/x)² = x² + 2 + 1/x².`,`Subtract 2: ${a}² − 2 = ${a*a-2}.`]);break;
case 12:if(i===0)set('Optimization',`A rectangle has perimeter ${4*n}. What is its largest possible area?`,n*n,'Express the area using one side, then complete the square.',[`If one side is x, the other is ${2*n} − x.`,`Area = x(${2*n} − x) = ${n*n} − (x − ${n})².`,`The maximum is ${n*n}, at a square.`]);else if(i===1)set('Inclusion–exclusion',`How many onto functions map a set of ${n} elements to a set of 2 elements?`,2**n-2,'Start with all functions and remove those missing an output.',[`Each input has 2 choices, giving 2^${n} functions.`,`Exactly 2 functions are constant and not onto.`,`Answer: ${2**n} − 2 = ${2**n-2}.`]);else set('Weighted binomial sum',`Find the sum of k × C(${n},k) for k = 0 through ${n}. Here C(n,k) counts k-element subsets.`,n*2**(n-1),'Count a subset with one distinguished member.',[`Choose the distinguished element in ${n} ways.`,`Each of the other ${n-1} elements may be in or out.`,`Total = ${n} × 2^${n-1} = ${n*2**(n-1)}.`]);break;
}
if(!Number.isFinite(answer)||!prompt)throw Error('Invalid problem configuration');
return {id:`${grade}-${kind}-${i}-${seed}`,grade,kind,topic,prompt,answer,hint,steps,template:i};
}
function parseAnswer(raw){let s=String(raw).trim().replace(/−/g,'-').replace(/,/g,'');if(s.length>80)return null;if(/^[+-]?(\d+(\.\d*)?|\.\d+)\s*\/\s*[+-]?(\d+(\.\d*)?|\.\d+)$/.test(s)){const [a,b]=s.split('/').map(Number);return b!==0&&Number.isFinite(a/b)?a/b:null;}if(!/^[+-]?(\d+(\.\d*)?|\.\d+)$/.test(s))return null;const x=Number(s);return Number.isFinite(x)?x:null;}
const api={realms,generate,parseAnswer,check:(raw,answer)=>{const n=parseAnswer(raw);return n!==null&&Math.abs(n-answer)<=1e-7*Math.max(1,Math.abs(answer));}};
if(typeof module!=='undefined'&&module.exports)module.exports=api;else root.StarMath=Object.freeze(api);
})(typeof window!=='undefined'?window:globalThis);
